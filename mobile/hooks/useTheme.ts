import {Colors} from "@/constants/Colors";

export default function useTheme() {
    //const colorScheme = useColorScheme();
    //const selected = colorScheme === "light" ? "light" : "dark";
    //return colors[selected];
    return Colors.dark;
}
